<select name="city" id="listado-de-opciones">
  <option value="config_1">Configuracion 1</option>
  <option value="config_2">Configuracion 2</option>
  <option value="config_3">Configuracion 3</option>
</select>

<%= form_with(model: region) do |form| %>
  <% if region.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(region.errors.count, "error") %> prohibited this region from being saved:</h2>

      <ul>
        <% region.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.select :country_id, @countries %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>

<%= javascript_pack_tag 'cambiar_seleccion' %>